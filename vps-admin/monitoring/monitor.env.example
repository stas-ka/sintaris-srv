# Sintaris Monitor — config template
# Copy to /opt/sintaris-monitor/.env on each VPS — NEVER commit with real values

# Telegram bot token (from BotFather)
BOT_TOKEN=

# Telegram chat ID to receive alerts (your user ID)
CHAT_ID=

# Human-readable label for this server in alert messages
HOSTNAME_LABEL=dev2null.de
